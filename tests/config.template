module.exports = {
  parallelTestsCount: __PARALLEL_TESTS__COUNT__,
  testFiles: `${__dirname}/project-tests/*.js`,
  project: {
    build() {
      __BUILD__
    },
    addListener(event) {},
  },
  mocha: {
    noColors: true,
  },
  ignoredBranches: ['__IGNORED_BRANCH__'],
  baseBranch: '__BASE_BRANCH__',
  dependencyTree: {
    filename: 'tests/app/app.js',
    directory: 'tests/app',
    filter: path => !path.includes('node_modules'),
  },
  ignoredFiles: ['**/*', '__IGNORED_FILES__'],
}
